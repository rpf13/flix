<section class="user">
  <%= profile_image(@user) %>
  <h1><%= @user.name %></h1>
  <h2><%= mail_to(@user.email) %></h2>
  <h3>Member Since</h3>
  <p>
    <strong><%= @user.created_at.strftime("%B %Y") %></strong>
  </p>
</section>